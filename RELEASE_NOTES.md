# Release Notes

This Garda Code Action build is rebranded for Neosantara and uses the `garda` Python package namespace.

For current project changes, see `CHANGELOG.md`.

Legacy upstream release notes were removed from this build to avoid stale branding and container image references.
